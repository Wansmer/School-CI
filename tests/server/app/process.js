const {describe, it} = require('mocha');
const { expect } = require('chai');

const { cloneRepo } = require('../../../server/app/process');

describe('Модуль process', () => {
  describe('Процедура cloneRepo', () => {

  })
});